<!DOCTYPE html>
<html>
  <head>
    <title>GriddleMeet</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', 'http://fonts.googleapis.com/css?family=Merriweather', media: 'all' %>
    <%= javascript_include_tag 'application' %>
    <%= yield :head %>
  </head>

  <body>
    <!-- Navbar component layout for all views -->
    <nav class="navbar navbar-inverse">
    <div class="container-fluid">
      <!-- Brand and toggle get grouped for better mobile display -->
      <div class="navbar-header">
        <a class="navbar-brand">GM</a>
      </div>

      <!-- Collect the nav links, forms, and other content for toggling -->
      <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
        <ul class="nav navbar-nav">
          <li class="active"><a href="/">Map <span class="sr-only">(current)</span></a></li>
          <% if user_signed_in? %>
          <li class="active"><%= link_to "My Trucks", user_trucks_path(current_user) %></li>
          <% end %>
        </ul>

        <ul class="nav navbar-nav navbar-right">
          <% if user_signed_in? %>
          <li class="name">
            <span class="sr-only">(current)</span>
            <%= current_user.name %>
          </li>
          <% end %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"> User Profile <span class="caret"></span></a>
            <ul class="dropdown-menu">
              <li><%= link_to "Sign In", new_user_session_path, class: "signin" %></li>
              <li><%= link_to "Sign Out", destroy_user_session_path, :method => :delete, class: "signout" %></li>
              <li><%= link_to "Sign Up", new_user_registration_path, class: "signout" %></li>
            </ul>
          </li>
        </ul>
      </div><!-- /.navbar-collapse -->
    </div><!-- /.container-fluid -->
  </nav>

    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>

    <%= yield %>
    <!-- Footer Section -->
      <footer class="footer">
      	&copy; Enjoy GriddleMeet || created by<span class="footer_link"><a target="_blank" href="https://github.com/kwadrnt"> Kevin Tse </a></span> || a WDI production
      </footer>
    </div>

  </body>
</html>
